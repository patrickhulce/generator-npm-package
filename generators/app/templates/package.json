{
  "name": "<%= name %>",
  "version": "0.0.0",
  "description": "<%= description %>",
  "main": "./lib/index.js",
  "scripts": {<% if (includeBrowserPackaging) { %>
    "clean": "rm -fR dist/",
    "build": "babel lib --out-dir dist/ && BABEL_ENV=rollup rollup -c rollup.config.js",<% } %>
    "test": "xo ./lib/**/*.js && mocha --reporter spec --require test/bootstrap test/*.test.js test/**/*.test.js",
    "test:lint": "xo ./lib/**/*.js",
    "test:coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- --require test/bootstrap test/*.test.js test/**/*.test.js",
    "test:watch": "mocha --watch --reporter dot --require test/bootstrap test/*.test.js test/**/*.test.js",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/patrickhulce/<%= name %>.git"
  },
  "author": "Patrick Hulce <patrick.hulce@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/patrickhulce/<%= name %>/issues"
  },
  "homepage": "https://github.com/patrickhulce/<%= name %>#readme",
  "xo": {
    "env": [
      "browser",
      "node",
      "mocha"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
