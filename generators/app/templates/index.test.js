const expect = require('chai').expect
const app = require('../lib')

describe('index.js', () => {
  describe('#app', () => {
    it('should work', () => {
      // expect(something).to.equal(2)
    })
  })
})
